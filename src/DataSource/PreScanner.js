// As a aggregagor to scan through profiles to collect the set of query parameter values for DataSource requests
export class PreScanner {
  constructor() {
    // should referenc server config to access profiles
  }
}

export default PreScanner;
